# PRD Helper 初始化配置（Setup）

| 配置项（Key） | 值（Value） |
| --- | --- |
| 文档目录（docs_root） | {docs_root} |
| 启用 Agent（enabled_agents） | {enabled_agents} |
| 采集策略（capture_policy） | {capture_policy} |
| 工作流（workflow） | 采集 Collect -> 精炼 Refine -> 关联 Relate -> 生成 Generate |

## 指令（Commands）

{command_list}

