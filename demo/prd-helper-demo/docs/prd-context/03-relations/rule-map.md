# 规则关联

## 规则清单

| relation_id | 关联规则 | 关联数据对象 | 关联功能 |
|-------------|---------|-------------|---------|
| rel-rule-001 | 采集规则 | 原始材料（source） | 采集 |
| rel-rule-002 | 精炼规则 | 事实（fact）、决策（decision）、约束（constraint）、冲突（conflict）、问题（question）、推断（assumption） | 精炼 |
| rel-rule-003 | 关联规则 | 关系（relation）、页面映射（page-map）、功能映射（feature-map）、规则映射（rule-map）、数据映射（data-map）、验收映射（acceptance-map） | 关联 |
| rel-rule-004 | 生成规则 | 项目说明（overview）、页面说明（page）、规则说明（rule）、数据说明（data）、验收标准（acceptance）、Agent上下文（agent-context） | 生成 |
| rel-rule-005 | 检查规则 | 检查报告（check） | 检查 |
| rel-rule-006 | Delta规则 | 增量记录（context-delta） | Context Delta |
